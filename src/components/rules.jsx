const Rules = ({ onClick }) => {
  return (
    <>
      <button
        className="rounded-md px-7 py-1 uppercase tracking-widest text-white outline outline-2 outline-headerOutline"
        onClick={onClick}
      >
        Rules
      </button>
    </>
  );
};

export default Rules;
